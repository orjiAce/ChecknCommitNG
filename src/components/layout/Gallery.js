import React from 'react';

const Gallery = () => {
    const {
        //authenticated,

        gallery
    } = props;


    return (
        <div>

        </div>
    );
};

export default Gallery;